{
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,tsx,php,css,scss,md,json}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.24.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.6.2"
  }
}
